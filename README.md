# Simulation for the MochiSwarm platform

Install dependencies:

```bash
pip install -r requirements.txt
```

Run the simulator:

```bash
python main.py
```

Optional: pass a MuJoCo XML path explicitly.

```bash
python main.py models/mochi_BlimpRace_balloon_course.xml
```
